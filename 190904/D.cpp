#include <iostream>
#include <string>

using namespace std;

int main(void) {
    int n, m;
    cin >> n >> m;
    string map[n];
    for (int i = 0; i < n; ++i) {
        cin >> map[i];
    }
    return 0;
}